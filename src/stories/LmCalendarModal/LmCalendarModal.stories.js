import React from 'react';
import { LmCalendarModal } from './LmCalendarModal';

export default {
  title: 'Lamarck/LmCalendarModal',
  component: LmCalendarModal,
  argTypes: {
    
  },
};

const Template = (args) => <LmCalendarModal {...args} />;

export const Base = Template.bind({});
Base.args = {
};
