import React from 'react';
import { LmAddGroupModal } from './LmAddGroupModal';

export default {
  title: 'Lamarck/LmAddGroupModal',
  component: LmAddGroupModal,
  argTypes: {
    
  },
};

const Template = (args) => <LmAddGroupModal {...args} />;

export const Base = Template.bind({});
Base.args = {
};
