import React from 'react';
import { LmMoveDiscardModal } from './LmMoveDiscardModal';

export default {
  title: 'Lamarck/LmMoveDiscardModal',
  component: LmMoveDiscardModal,
  argTypes: {
    
  },
};

const Template = (args) => <LmMoveDiscardModal {...args} />;

export const MoveCard = Template.bind({});
MoveCard.args = {
};
